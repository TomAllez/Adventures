export const TICK_RATE = 20;
export const TICK_INTERVAL_MS = 1000 / TICK_RATE;

export const PLAYER_SPEED = 200; // pixels per second
export const PLAYER_RADIUS = 14; // collision half-size in pixels

export const MAP_TILE_SIZE = 32;
export const MAP_COLS = 50;
export const MAP_ROWS = 28;

export const WORLD_WIDTH = MAP_COLS * MAP_TILE_SIZE;  // 1600
export const WORLD_HEIGHT = MAP_ROWS * MAP_TILE_SIZE; // 896
